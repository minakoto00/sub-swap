---
phase: 01-code-quality-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/cli.rs
  - src/lib.rs
  - src/main.rs
  - justfile
autonomous: true
requirements: [QUAL-04, OBSV-01, OBSV-02]

must_haves:
  truths:
    - "cargo build --lib succeeds after lib.rs is added"
    - "cargo build succeeds (binary still works)"
    - "just check, just test, just lint, just fmt each work as standalone commands"
    - "just validate runs fmt-check then clippy then test and stops on first failure"
    - "cargo fmt --check passes with no diff after formatting is applied"
    - "All 45 existing tests still pass"
  artifacts:
    - path: "src/lib.rs"
      provides: "Library target re-exporting all modules"
      contains: "pub mod cli"
      min_lines: 8
    - path: "src/main.rs"
      provides: "Thin binary entry point importing from library crate"
      contains: "use sub_swap::cli"
    - path: "justfile"
      provides: "Command runner with check, test, lint, fmt, validate recipes"
      contains: "validate:"
  key_links:
    - from: "src/main.rs"
      to: "src/lib.rs"
      via: "use sub_swap::cli::{Cli, run}"
      pattern: "use sub_swap::cli"
    - from: "src/cli.rs"
      to: "src/main.rs"
      via: "Cli/Commands/ConfigAction defined in cli.rs, imported by main.rs"
      pattern: "pub struct Cli"
    - from: "justfile"
      to: "cargo"
      via: "Each recipe delegates to cargo subcommands"
      pattern: "cargo fmt --check && cargo clippy"
---

<objective>
Create the library target (lib.rs), the justfile command runner, and apply cargo fmt to establish the complete zero-violation quality baseline.

Purpose: lib.rs enables Phase 2 structural tests and cargo doc. The justfile provides standardized developer ergonomics commands. Applying cargo fmt in a separate commit (per D-07) completes the formatting baseline.
Output: src/lib.rs, updated src/main.rs and src/cli.rs, justfile, and formatted codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-quality-foundation/01-CONTEXT.md
@.planning/phases/01-code-quality-foundation/01-RESEARCH.md
@.planning/phases/01-code-quality-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output that this plan depends on. -->
<!-- Plan 01 fixes clippy violations and adds [lints] table — no new types created. -->
<!-- This plan needs the current structure of cli.rs, main.rs to know what to move. -->

From src/main.rs (BEFORE this plan modifies it):
```rust
// These three types must MOVE from main.rs to cli.rs:
pub struct Cli { pub command: Option<Commands> }
pub enum Commands { List{..}, Use{..}, Add{..}, Remove{..}, Rename{..}, Note{..}, Decrypt{..}, Config{..} }
pub enum ConfigAction { Set{..}, Show }
```

From src/cli.rs line 11 (BEFORE this plan modifies it):
```rust
// This import references main.rs types — will break when lib.rs is added:
use crate::{Cli, Commands, ConfigAction};
// Must be REMOVED after types move into cli.rs itself.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move types to cli.rs, create lib.rs, update main.rs</name>
  <files>src/cli.rs, src/lib.rs, src/main.rs</files>

  <read_first>
    src/main.rs
    src/cli.rs
    .planning/phases/01-code-quality-foundation/01-RESEARCH.md
  </read_first>

  <action>
This is the critical refactoring step per QUAL-04 and Pitfall 1 from RESEARCH.md. Three files change:

**1. Move `Cli`, `Commands`, `ConfigAction` from `src/main.rs` into `src/cli.rs` (per D-03/D-04):**

Cut the following from `src/main.rs`:
- The `use clap::{Parser, Subcommand};` import
- The `#[derive(Parser)] pub struct Cli { ... }` block (lines 13-17)
- The `#[derive(Subcommand)] pub enum Commands { ... }` block (lines 19-53)
- The `#[derive(Subcommand)] pub enum ConfigAction { ... }` block (lines 55-61)

Paste them into `src/cli.rs` at the TOP of the file (before existing imports). Then:
- In `src/cli.rs`, add `use clap::{Parser, Subcommand};` at the top if not already present (it is not — cli.rs currently has no clap imports)
- In `src/cli.rs`, REMOVE the line `use crate::{Cli, Commands, ConfigAction};` (line 11) since the types are now defined locally in cli.rs
- Ensure `Cli`, `Commands`, and `ConfigAction` remain `pub` so they're accessible from main.rs and from lib.rs

**2. Create `src/lib.rs` (per D-03 — thin re-export module):**

Create a new file `src/lib.rs` with exactly this content:
```rust
pub mod cli;
pub mod config;
pub mod crypto;
pub mod error;
pub mod guard;
pub mod paths;
pub mod profile;
pub mod tui;
```

No additional code, no `#![warn(...)]` attributes (lints are in Cargo.toml per Research "State of the Art"), no logic.

**3. Rewrite `src/main.rs` as thin entry point (per D-04):**

Replace `src/main.rs` entirely with:
```rust
use clap::Parser;
use sub_swap::cli::{run, Cli};

fn main() {
    let cli = Cli::parse();
    if let Err(e) = run(cli) {
        eprintln!("Error: {e}");
        std::process::exit(1);
    }
}
```

Remove all `mod` declarations from main.rs — they now live in lib.rs.

**4. Verify immediately after changes:**
- `cargo build --lib` must succeed (library target compiles)
- `cargo build` must succeed (binary target compiles)
- `cargo test` must pass (all 45 tests)
- `cargo clippy -- -D warnings` must pass (no new warnings introduced)

If `cargo build --lib` fails with "use of undeclared type" errors, it means types weren't properly moved to cli.rs or imports are wrong. Check that `use crate::{Cli, Commands, ConfigAction}` was removed from cli.rs.
  </action>

  <verify>
    <automated>cd /Users/brainco/security/sub-swap && export PATH="$HOME/.cargo/bin:$PATH" && cargo build --lib 2>&1 | tail -3 && cargo test 2>&1 | tail -5 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - src/lib.rs exists and contains `pub mod cli;` through `pub mod tui;` (8 modules)
    - src/lib.rs does NOT contain any `#![warn` or `#![allow` attributes
    - src/main.rs contains `use sub_swap::cli::{run, Cli};`
    - src/main.rs does NOT contain `mod cli;` or any other `mod` declarations
    - src/main.rs is under 15 lines total
    - src/cli.rs contains `pub struct Cli` (moved from main.rs)
    - src/cli.rs contains `pub enum Commands` (moved from main.rs)
    - src/cli.rs contains `pub enum ConfigAction` (moved from main.rs)
    - src/cli.rs does NOT contain `use crate::{Cli, Commands, ConfigAction}`
    - src/cli.rs contains `use clap::{Parser, Subcommand};`
    - `cargo build --lib` exits 0
    - `cargo build` exits 0
    - `cargo test` exits 0 (all 45 tests pass)
    - `cargo clippy -- -D warnings` exits 0
  </acceptance_criteria>

  <done>lib.rs exists as thin re-export module. main.rs is a thin entry point importing from `sub_swap::cli`. Cli/Commands/ConfigAction live in cli.rs. Both library and binary targets compile. All tests pass. Clippy passes.</done>
</task>

<task type="auto">
  <name>Task 2: Install just and create justfile</name>
  <files>justfile</files>

  <read_first>
    .planning/phases/01-code-quality-foundation/01-RESEARCH.md
    .planning/phases/01-code-quality-foundation/01-CONTEXT.md
  </read_first>

  <action>
Per OBSV-01 and OBSV-02, and decisions D-05/D-06.

**1. Install `just` command runner:**

Run `brew install just` (preferred per RESEARCH.md). If brew is unavailable, fall back to `cargo install just`. Verify installation with `just --version`.

**2. Create `justfile` at project root:**

```just
# justfile

# Fast compile check
check:
    cargo check

# Run all tests
test:
    cargo test

# Run clippy with warnings as errors
lint:
    cargo clippy -- -D warnings

# Apply formatting in-place
fmt:
    cargo fmt

# Validate all quality gates in sequence; stops on first failure
validate:
    cargo fmt --check && cargo clippy -- -D warnings && cargo test
```

Exactly 5 recipes per D-05: `check`, `test`, `lint`, `fmt`, `validate`. No extras.

The `validate` recipe uses `&&` chaining per D-06 so it stops on first failure. No `set shell` line (the default is fine for this project).

**3. Verify each recipe works:**
- `just check` — should exit 0
- `just lint` — should exit 0 (clippy passes from Plan 01)
- `just test` — should exit 0 (all tests pass)
- `just fmt` — applies formatting (may change files, that's expected — formatting commit is Task 3)
- `just validate` — will FAIL at `cargo fmt --check` if formatting hasn't been applied yet. That's expected. Verify it stops at the first failure (fmt check) and does NOT proceed to clippy/test.
  </action>

  <verify>
    <automated>cd /Users/brainco/security/sub-swap && export PATH="$HOME/.cargo/bin:$PATH" && just --version && just check 2>&1 | tail -3 && just lint 2>&1 | tail -3 && just test 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `just --version` outputs a version string (just is installed)
    - justfile exists at project root
    - justfile contains recipe `check:` with body `cargo check`
    - justfile contains recipe `test:` with body `cargo test`
    - justfile contains recipe `lint:` with body `cargo clippy -- -D warnings`
    - justfile contains recipe `fmt:` with body `cargo fmt`
    - justfile contains recipe `validate:` with body `cargo fmt --check && cargo clippy -- -D warnings && cargo test`
    - `just check` exits 0
    - `just lint` exits 0
    - `just test` exits 0
  </acceptance_criteria>

  <done>just is installed. justfile exists with all 5 required recipes (check, test, lint, fmt, validate). Each standalone recipe works. validate uses && chaining that stops on first failure.</done>
</task>

<task type="auto">
  <name>Task 3: Apply cargo fmt and verify full validation</name>
  <files></files>

  <read_first>
    rustfmt.toml
  </read_first>

  <action>
Per D-07, formatting changes go in a separate commit from config/source changes.

**1. Run `cargo fmt` to reformat the entire codebase:**

```bash
cargo fmt
```

This applies the rustfmt.toml config (edition 2021, max_width 100). The RESEARCH.md identified 32 format diff chunks across 8 files — mostly import reordering and argument wrapping. This is a mechanical, no-logic change.

**2. Verify the formatting is clean:**

```bash
cargo fmt --check
```

Must exit 0 with no diff.

**3. Verify nothing broke:**

```bash
cargo test
cargo clippy -- -D warnings
```

Both must pass. Formatting changes should never break tests or introduce clippy warnings.

**4. Run the full validation pipeline:**

```bash
just validate
```

This runs: `cargo fmt --check && cargo clippy -- -D warnings && cargo test`

Must exit 0. This is the final gate for Phase 1's success criteria.

**5. Commit the formatting changes separately (per D-07):**

The executor should commit this as a separate commit from the config/source-fix commit. The commit message should indicate it's a mechanical formatting pass, e.g., `style(01-02): apply cargo fmt to entire codebase`.
  </action>

  <verify>
    <automated>cd /Users/brainco/security/sub-swap && export PATH="$HOME/.cargo/bin:$PATH" && cargo fmt --check 2>&1 && just validate 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `cargo fmt --check` exits 0 (no formatting diff remains)
    - `cargo clippy -- -D warnings` exits 0
    - `cargo test` exits 0 (all 45 tests pass)
    - `just validate` exits 0 (full pipeline passes end-to-end)
  </acceptance_criteria>

  <done>Entire codebase formatted per rustfmt.toml. `just validate` passes end-to-end: fmt check, clippy, and all tests green. Phase 1 quality baseline is complete.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cargo build --lib` exits 0 (library target works)
2. `cargo build` exits 0 (binary target works)
3. `cargo fmt --check` exits 0 (no formatting violations)
4. `cargo clippy -- -D warnings` exits 0 (no lint violations)
5. `cargo test` exits 0 (all 45 tests pass)
6. `just validate` exits 0 (full pipeline green)
7. `just check`, `just test`, `just lint`, `just fmt` each work standalone
8. src/lib.rs exists as thin re-export
9. justfile exists with 5 recipes
</verification>

<success_criteria>
- lib.rs exists and `cargo build --lib` succeeds
- justfile exists with check/test/lint/fmt/validate recipes, all working
- `just validate` passes end-to-end (fmt-check + clippy + test)
- Formatting committed separately per D-07 two-commit strategy
- All 45 existing tests pass throughout
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-quality-foundation/01-02-SUMMARY.md`
</output>
