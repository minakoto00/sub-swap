---
phase: 04-agent-entry-point
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - HEALTH.md
  - src/cli.rs
  - src/tui/mod.rs
  - tests/arch.rs
autonomous: true
requirements:
  - DOCS-05
  - OBSV-03

must_haves:
  truths:
    - "CLAUDE.md is under 80 lines"
    - "CLAUDE.md Build & Test Commands section is present verbatim (lines 5-18 of original)"
    - "CLAUDE.md has a pointer table linking to every file in docs/"
    - "CLAUDE.md Key constraints section is present inline with 4 bullet points"
    - "HEALTH.md exists with status indicators for all 5 domains (crypto, profile, TUI, docs, enforcement)"
    - "HEALTH.md accurately reflects Phase 1-3 outcomes"
    - "just validate passes (cargo fmt --check + clippy + test)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Concise agent navigation map under 80 lines"
      contains: "## Build & Test Commands"
    - path: "HEALTH.md"
      provides: "Machine-readable quality scorecard for 5 domains"
      contains: "## Health"
  key_links:
    - from: "CLAUDE.md"
      to: "docs/ARCHITECTURE.md"
      via: "markdown link in pointer table"
      pattern: "\\[docs/ARCHITECTURE\\.md\\]"
    - from: "CLAUDE.md"
      to: "docs/SECURITY.md"
      via: "markdown link in pointer table"
      pattern: "\\[docs/SECURITY\\.md\\]"
    - from: "CLAUDE.md"
      to: "docs/TESTING.md"
      via: "markdown link in pointer table"
      pattern: "\\[docs/TESTING\\.md\\]"
---

<objective>
Restructure CLAUDE.md as a concise navigation map under 80 lines and create HEALTH.md as a machine-readable quality scorecard.

Purpose: Make CLAUDE.md the single entry point for agents — build commands and guardrails up front, then a pointer table to deep-dive docs. HEALTH.md grades all domains so agents (and the developer) can see project health at a glance.

Output: Rewritten CLAUDE.md (~42 lines), new HEALTH.md (~15 lines), and clean formatting baseline via cargo fmt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-entry-point/04-CONTEXT.md
@.planning/phases/04-agent-entry-point/04-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix formatting and restructure CLAUDE.md as navigation map</name>
  <files>CLAUDE.md, src/cli.rs, src/tui/mod.rs, tests/arch.rs</files>
  <read_first>
    - CLAUDE.md (current file being rewritten — must see exact content to preserve Build & Test Commands verbatim)
    - docs/ARCHITECTURE.md (verify link target exists, read first line for purpose description)
    - docs/SECURITY.md (verify link target exists, read first line for purpose description)
    - docs/TESTING.md (verify link target exists, read first line for purpose description)
    - docs/decisions/001-aes-256-gcm.md (verify link target exists)
    - docs/decisions/002-os-keychain.md (verify link target exists)
    - docs/decisions/003-path-injection.md (verify link target exists)
    - docs/decisions/004-offline-only.md (verify link target exists)
    - justfile (confirm just commands exist for optional inclusion)
  </read_first>
  <action>
**Step 1 — Fix formatting (Wave 0 prerequisite):**

Run `cargo fmt` to fix the three formatting violations identified in RESEARCH.md:
- `src/cli.rs:331` — trailing newline
- `src/tui/mod.rs:626` — trailing newline
- `tests/arch.rs:97,212` — line-length array formatting and multi-line parse chain

Then verify: `cargo fmt --check` exits 0.

**Step 2 — Rewrite CLAUDE.md:**

Replace the entire file with the following structure (~42 lines). The Build & Test Commands section (lines 5-18 of current file) MUST be preserved character-for-character. The Key constraints section (lines 50-55 of current file) MUST be preserved inline.

Target structure:

```
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build & Test Commands

[VERBATIM copy of current lines 7-18 — the code block with all cargo commands]

## Key Constraints

- **Strictly offline**: No network crates in dependency tree. No async runtime.
- **File permissions**: All files under `~/.sub-swap/` must be created with mode 0600 (Unix). Check `#[cfg(unix)]` blocks in `config.rs`, `profile/store.rs`, and `profile/switch.rs`.
- **`decrypt` command is view-only**: Prints to stdout, never writes decrypted data to disk.
- **Process guard**: `sub-swap use` checks for running Codex processes via sysinfo and blocks unless `--force` is passed.

## Architecture

sub-swap manages multiple `~/.codex/` profiles (auth.json + config.toml) with AES-256-GCM encryption at rest. Two entry modes (CLI and TUI) share core logic through layered modules. See docs/ for full details.

## Documentation

| File | Purpose |
|------|---------|
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | Module layout, layer rules, dependency graph, enforcement tests |
| [docs/SECURITY.md](docs/SECURITY.md) | Encryption model, key management, threat model, file permissions |
| [docs/TESTING.md](docs/TESTING.md) | Test infrastructure, mocks, recipes for adding tests |
| [docs/decisions/001-aes-256-gcm.md](docs/decisions/001-aes-256-gcm.md) | ADR: AES-256-GCM for authenticated encryption |
| [docs/decisions/002-os-keychain.md](docs/decisions/002-os-keychain.md) | ADR: OS keychain for encryption key storage |
| [docs/decisions/003-path-injection.md](docs/decisions/003-path-injection.md) | ADR: path injection for test isolation |
| [docs/decisions/004-offline-only.md](docs/decisions/004-offline-only.md) | ADR: strictly offline constraint |
```

Per D-01: The detailed Architecture section (current lines 20-48 covering CLI/TUI/wizard entry points, testability patterns, encryption flow, switch lifecycle, input validation) is replaced with the 1-2 sentence summary above. All that detail now lives in docs/ARCHITECTURE.md and docs/SECURITY.md.

Per D-02: The Build & Test Commands code block is copied verbatim — same indentation, same comments, same line breaks.

Per D-03: Key constraints remain inline as 4 bullet points — these are "stop first" guardrails agents need before following any link.

Per D-04: Pointer table uses `| File | Purpose |` columns with markdown links.

Per D-05: Each row has a one-line purpose description.

**Claude's discretion choices:**
- Section ordering: Build & Test Commands first (most immediately useful), then Key Constraints (guardrails), then Architecture summary, then Documentation pointer table. This ensures an agent sees actionable information before navigational information.
- justfile commands: Add a brief note after the cargo code block mentioning `just validate` as the combined quality gate. This costs 2 lines but makes the primary quality check discoverable. Example: add a second small code block with `just validate  # fmt + clippy + test in sequence`.
- Exact architecture summary wording: Use the sentence shown above — it captures what sub-swap does, what encryption it uses, and that CLI/TUI share core logic.

Verify final line count with `wc -l CLAUDE.md` — must be under 80.
  </action>
  <verify>
    <automated>export PATH="$HOME/.cargo/bin:$PATH" && cargo fmt --check && wc -l CLAUDE.md | awk '{if ($1 < 80) print "PASS: "$1" lines"; else print "FAIL: "$1" lines"; exit ($1 >= 80)}'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0 (no formatting violations remain)
    - `wc -l CLAUDE.md` outputs a number less than 80
    - `grep -c "## Build & Test Commands" CLAUDE.md` outputs 1
    - `grep -c "cargo test" CLAUDE.md` outputs at least 3 (the test commands from the verbatim section)
    - `grep -c "## Key Constraints" CLAUDE.md` outputs 1 (section header present; note: may be "Key constraints" lowercase — either is acceptable as long as the 4 bullet points are present)
    - `grep "Strictly offline" CLAUDE.md` matches (first constraint bullet present)
    - `grep "File permissions" CLAUDE.md` matches (second constraint bullet present)
    - `grep "decrypt.*view-only" CLAUDE.md` matches (third constraint bullet present)
    - `grep "Process guard" CLAUDE.md` matches (fourth constraint bullet present)
    - `grep "docs/ARCHITECTURE.md" CLAUDE.md` matches (pointer table links to ARCHITECTURE.md)
    - `grep "docs/SECURITY.md" CLAUDE.md` matches (pointer table links to SECURITY.md)
    - `grep "docs/TESTING.md" CLAUDE.md` matches (pointer table links to TESTING.md)
    - `grep "docs/decisions/001-aes-256-gcm.md" CLAUDE.md` matches (ADR 1 linked)
    - `grep "docs/decisions/002-os-keychain.md" CLAUDE.md` matches (ADR 2 linked)
    - `grep "docs/decisions/003-path-injection.md" CLAUDE.md` matches (ADR 3 linked)
    - `grep "docs/decisions/004-offline-only.md" CLAUDE.md` matches (ADR 4 linked)
    - `grep -c "## Documentation" CLAUDE.md` outputs 1 (pointer table section exists)
    - Lines 20-48 of the OLD CLAUDE.md (detailed architecture prose) do NOT appear in the new file — no "Testability patterns", "Encryption flow", "Profile switch lifecycle", or "Input validation" subsections
  </acceptance_criteria>
  <done>CLAUDE.md is under 80 lines, retains Build & Test Commands verbatim, retains Key Constraints inline, has a 1-2 sentence architecture summary, and has a 7-row pointer table linking to all docs/ files. Formatting violations in src/cli.rs, src/tui/mod.rs, and tests/arch.rs are fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Create HEALTH.md quality scorecard</name>
  <files>HEALTH.md</files>
  <read_first>
    - CLAUDE.md (just rewritten in Task 1 — verify it exists and is under 80 lines)
    - justfile (verify just validate recipe exists)
    - tests/arch.rs (count structural tests for enforcement domain detail)
    - docs/ARCHITECTURE.md (confirm exists for docs domain)
    - docs/SECURITY.md (confirm exists for docs domain)
    - docs/TESTING.md (confirm exists for docs domain)
  </read_first>
  <action>
**Before writing HEALTH.md, verify the quality baseline:**

1. Run `just validate` (which runs `cargo fmt --check && cargo clippy -- -D warnings && cargo test`) — it MUST pass. If it does not pass, fix the issue before proceeding. Task 1 should have fixed formatting.

2. Count test results: run `cargo test 2>&1 | tail -5` to get the final test summary line (e.g., "test result: ok. 56 passed; 0 failed").

3. Count structural tests specifically: `grep -c "#\[test\]" tests/arch.rs` to get the arch test count.

**Then create HEALTH.md with this exact content (adjusting numbers from the counts above):**

```markdown
# Health

**Updated:** 2026-04-08 (after Phase 4)

| Domain | Status | Detail |
|--------|--------|--------|
| crypto | ✅ | AES-256-GCM encrypt/decrypt, key management via OS keychain; unit tests passing |
| profile | ✅ | Switch lifecycle, store, path-injected isolation; unit + integration tests passing |
| TUI | ⚠️ | Functional but no automated widget tests (ratatui testing deferred — out of scope) |
| docs | ✅ | ARCHITECTURE.md, SECURITY.md, TESTING.md, 4 ADRs in docs/decisions/ |
| enforcement | ✅ | {N} arch tests + clippy clean + fmt clean; `just validate` passes |
```

Replace `{N}` with the actual count from `grep -c "#\[test\]" tests/arch.rs` (expected: 11).

Per D-06: Grade exactly 5 domains — crypto, profile, TUI, docs, enforcement.

Per D-07: Use emoji status indicators in the table — checkmark for passing, warning for partial, cross for failing. Based on verified Phase 1-3 outcomes:
- crypto: checkmark (9+ unit tests, all passing)
- profile: checkmark (unit + integration tests, all passing)
- TUI: warning (no automated widget tests — this is a documented known gap, TUI test infrastructure is explicitly out of scope per REQUIREMENTS.md)
- docs: checkmark (all 3 core docs + 4 ADRs created in Phase 3)
- enforcement: checkmark (arch tests pass, clippy clean, fmt clean after Task 1 fix)

Per D-08: Scores reflect Phase 1-3 outcomes — lint config (Phase 1), structural tests (Phase 2), documentation (Phase 3).

Per D-09: Manual update at phase transitions. No automation script. Include "Updated" date with phase reference.

**Claude's discretion choices:**
- Include "Updated" timestamp with phase reference: `2026-04-08 (after Phase 4)`
- Status descriptions are one-line summaries of what exists and what evidence supports the grade
  </action>
  <verify>
    <automated>export PATH="$HOME/.cargo/bin:$PATH" && test -f HEALTH.md && grep -c "✅" HEALTH.md | awk '{if ($1 == 4) print "PASS: 4 passing domains"; else print "FAIL: expected 4 passing, got "$1}' && grep -c "⚠️" HEALTH.md | awk '{if ($1 == 1) print "PASS: 1 warning domain"; else print "FAIL: expected 1 warning, got "$1}' && just validate</automated>
  </verify>
  <acceptance_criteria>
    - `test -f HEALTH.md` exits 0 (file exists)
    - `head -1 HEALTH.md` outputs `# Health`
    - `grep "Updated:" HEALTH.md` matches (timestamp line present)
    - `grep "2026-04-08" HEALTH.md` matches (current date in updated field)
    - `grep "Phase 4" HEALTH.md` matches (phase reference in updated field)
    - `grep "crypto" HEALTH.md` matches (crypto domain row present)
    - `grep "profile" HEALTH.md` matches (profile domain row present)
    - `grep "TUI" HEALTH.md` matches (TUI domain row present)
    - `grep "docs" HEALTH.md` matches (docs domain row present)
    - `grep "enforcement" HEALTH.md` matches (enforcement domain row present)
    - `grep -c "✅" HEALTH.md` outputs 4 (crypto, profile, docs, enforcement all passing)
    - `grep -c "⚠️" HEALTH.md` outputs 1 (TUI is the only warning)
    - `grep -c "❌" HEALTH.md` outputs 0 (no failing domains)
    - `just validate` exits 0 (full quality gate passes — confirms enforcement domain is accurately scored)
  </acceptance_criteria>
  <done>HEALTH.md exists with 5 graded domains (crypto, profile, TUI, docs, enforcement), accurate status indicators reflecting Phase 1-3 outcomes, and `just validate` passes confirming the enforcement score is truthful.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md content | Agents read CLAUDE.md as their primary instruction source; misleading content could direct agents to skip security checks |
| HEALTH.md accuracy | Inaccurate health scores could cause agents to skip verification of failing domains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | CLAUDE.md | accept | CLAUDE.md contains no secrets — it is a navigation map pointing to public docs. No sensitive data to disclose. |
| T-04-02 | Tampering | HEALTH.md scores | mitigate | HEALTH.md scores are verified against `just validate` output before writing. Task 2 action explicitly requires running `just validate` and confirming exit 0 before scoring enforcement as passing. |
| T-04-03 | Repudiation | CLAUDE.md restructure | accept | CLAUDE.md restructure is tracked in git — the old version is recoverable from commit history. Low-value target for repudiation. |
| T-04-04 | Denial of Service | CLAUDE.md removal of security guidance | mitigate | Key constraints section (offline-only, 0600 permissions, decrypt view-only, process guard) is kept inline per D-03, not removed. docs/SECURITY.md link in pointer table ensures full threat model remains accessible. Acceptance criteria verify all 4 constraint bullets are present. |
</threat_model>

<verification>
1. `wc -l CLAUDE.md` outputs a number under 80
2. `grep "## Build & Test Commands" CLAUDE.md` matches
3. `grep "docs/ARCHITECTURE.md" CLAUDE.md` matches
4. `grep "docs/SECURITY.md" CLAUDE.md` matches
5. `grep "docs/TESTING.md" CLAUDE.md` matches
6. `grep "Strictly offline" CLAUDE.md` matches
7. `test -f HEALTH.md` exits 0
8. `grep -c "✅" HEALTH.md` outputs 4
9. `grep -c "⚠️" HEALTH.md` outputs 1
10. `just validate` exits 0
</verification>

<success_criteria>
- CLAUDE.md is a concise navigation map under 80 lines
- CLAUDE.md retains Build & Test Commands verbatim and Key Constraints inline
- CLAUDE.md has a pointer table linking to all 7 docs/ files (3 core + 4 ADRs)
- HEALTH.md grades all 5 domains with accurate status indicators
- All quality gates pass (`just validate` exits 0)
- An agent starting a new session can find any architectural constraint by following a CLAUDE.md link
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-entry-point/04-01-SUMMARY.md`
</output>
