---
phase: 03-documentation-knowledge-base
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/TESTING.md
  - docs/decisions/001-aes-256-gcm.md
  - docs/decisions/002-os-keychain.md
  - docs/decisions/003-path-injection.md
  - docs/decisions/004-offline-only.md
autonomous: true
requirements: [DOCS-03, DOCS-04]
must_haves:
  truths:
    - "An agent reading docs/TESTING.md can write a new unit test using Paths::from_temp, MockKeyStore, or MockGuard by copying the provided templates"
    - "An agent reading docs/TESTING.md can add a new arch boundary rule to tests/arch.rs by following the recipe"
    - "An agent reading any ADR can understand the decision, its consequences, and which source files implement it"
    - "docs/decisions/ contains exactly 4 ADR files with the specified filenames"
  artifacts:
    - path: "docs/TESTING.md"
      provides: "Test patterns with copyable templates, recipes for adding new tests, structural test extension guide"
      min_lines: 120
      contains: "## Constraints"
    - path: "docs/decisions/001-aes-256-gcm.md"
      provides: "ADR for AES-256-GCM encryption choice"
      min_lines: 25
      contains: "## Decision"
    - path: "docs/decisions/002-os-keychain.md"
      provides: "ADR for OS keychain key storage choice"
      min_lines: 25
      contains: "## Decision"
    - path: "docs/decisions/003-path-injection.md"
      provides: "ADR for path injection testability pattern"
      min_lines: 25
      contains: "## Decision"
    - path: "docs/decisions/004-offline-only.md"
      provides: "ADR for offline-only constraint"
      min_lines: 25
      contains: "## Decision"
  key_links:
    - from: "docs/TESTING.md"
      to: "src/paths.rs"
      via: "copyable Paths::from_temp template with required directory creation"
      pattern: "from_temp"
    - from: "docs/TESTING.md"
      to: "tests/arch.rs"
      via: "recipe for adding new boundary rules using assert_no_crate_import"
      pattern: "assert_no_crate_import"
    - from: "docs/decisions/001-aes-256-gcm.md"
      to: "src/crypto/mod.rs"
      via: "implementation reference"
      pattern: "crypto/mod.rs"
---

<objective>
Create docs/TESTING.md with copyable test templates and recipes, plus 4 ADRs in docs/decisions/.

Purpose: TESTING.md gives agents the exact patterns to write new tests without exploring the codebase. ADRs document the four settled design choices so future agents understand why these decisions were made and which files implement them.

Output: Five Markdown files — one test guide and four decision records.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documentation-knowledge-base/03-CONTEXT.md
@.planning/phases/03-documentation-knowledge-base/03-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/TESTING.md</name>
  <files>docs/TESTING.md</files>
  <read_first>
    - src/paths.rs (Paths struct, from_temp constructor, #[cfg(test)] gate, field names: codex_dir, sub_swap_dir, profiles_dir())
    - src/crypto/keychain.rs (KeyStore trait, MockKeyStore implementation, #[cfg(test)] gate, new/set_key/get_key methods)
    - src/guard.rs (CodexGuard trait, MockGuard implementation, #[cfg(test)] gate, new/check methods)
    - tests/arch.rs (read_source, assert_no_crate_import, dep_names helpers; all 11 test names; NETWORK_CRATES constant; single-line import assumption comment)
    - tests/integration.rs (env!("CARGO_BIN_EXE_sub-swap") pattern, Command usage)
    - .planning/phases/03-documentation-knowledge-base/03-RESEARCH.md (§TESTING.md Content for verified patterns, §Code Examples for exact copyable templates, §Common Pitfalls 2-3 for accuracy traps)
  </read_first>
  <action>
Create `docs/TESTING.md` with the following exact structure and content (per D-01 agent-first, D-02 tables, D-10 template-based, D-11 recipe, D-12 structural tests):

**Section 1: Constraints and Invariants (lines 1-50)**

Open with `# Testing` title, then `## Constraints and Invariants` containing a table:

| Rule | Mechanism | Notes |
|------|-----------|-------|
| Tests never touch real `~/.codex/` or `~/.sub-swap/` | `Paths::from_temp(tmp.path())` injects temp directory | Every test that does file I/O must use this |
| Tests never access real OS keychain | `MockKeyStore` backed by `RefCell<Option<[u8; 32]>>` | `get_key()` before `set_key()` returns Err |
| Tests never detect real processes | `MockGuard` with fixed PID list | `MockGuard::new(vec![])` for clean check, `MockGuard::new(vec![pid])` for blocked |
| `MockKeyStore`, `MockGuard`, `Paths::from_temp` are `#[cfg(test)]` only | Compile-time gating | Not available in production builds |
| Unit tests live in `#[cfg(test)] mod tests` inside the source file | Rust convention | Tests for `crypto/mod.rs` go in `src/crypto/mod.rs` |
| Integration tests use compiled binary via `std::process::Command` | `env!("CARGO_BIN_EXE_sub-swap")` | `cargo test --test integration` builds automatically |
| Structural tests parse source at runtime | `tests/arch.rs` reads `.rs` files with `std::fs::read_to_string` | See "Adding Architectural Rules" recipe below |

Follow with a boxed note:
```
> **IMPORTANT:** All three test abstractions (`Paths::from_temp`, `MockKeyStore`, `MockGuard`)
> are gated behind `#[cfg(test)]`. They are only available when running `cargo test`, not in
> production builds. Code examples below must appear inside `#[cfg(test)] mod tests { }` blocks.
```

**Section 2: Test Infrastructure**

`## Test Infrastructure` with three subsections, each providing a copyable template (per D-10):

`### Paths Injection — Paths::from_temp`
- Source: `src/paths.rs` (constructor gated by `#[cfg(test)]`)
- What it does: Maps `base/codex` to `~/.codex` equivalent and `base/sub-swap` to `~/.sub-swap` equivalent within a temp directory
- Template (copy exactly from RESEARCH.md §Code Examples "Minimal Unit Test With Paths Injection"):
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::paths::Paths;
    use tempfile::TempDir;

    #[test]
    fn test_example() {
        let tmp = TempDir::new().unwrap();
        let paths = Paths::from_temp(tmp.path());
        std::fs::create_dir_all(&paths.sub_swap_dir).unwrap();
        std::fs::create_dir_all(&paths.codex_dir).unwrap();
        // ... test using &paths
    }
}
```
- Note: Callers must manually create required subdirectories. `Paths::from_temp` creates the mapping but not the directories themselves.

`### Keychain Mock — MockKeyStore`
- Source: `src/crypto/keychain.rs` (gated by `#[cfg(test)]`)
- What it does: In-memory keychain backed by `RefCell<Option<[u8; 32]>>`. Starts with no key stored.
- Template (copy exactly from RESEARCH.md §Code Examples "Unit Test With MockKeyStore"):
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::crypto::{generate_key, keychain::MockKeyStore};

    #[test]
    fn test_with_mock_keystore() {
        let store = MockKeyStore::new();
        let key = generate_key();
        store.set_key(&key).expect("set_key should succeed");
        let retrieved = store.get_key().expect("get_key should succeed");
        assert_eq!(retrieved, key);
    }
}
```
- Behavior: `get_key()` before `set_key()` returns `Err` (simulates missing keychain entry).

`### Process Guard Mock — MockGuard`
- Source: `src/guard.rs` (gated by `#[cfg(test)]`)
- What it does: Takes a fixed `Vec<u32>` of PIDs at construction. `check()` returns `Ok(())` if empty, `Err(CodexRunning(pids))` if non-empty.
- Template (copy exactly from RESEARCH.md §Code Examples "Unit Test With MockGuard"):
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_guard_blocks_when_codex_running() {
        let guard = MockGuard::new(vec![99999]);
        let result = guard.check();
        assert!(result.is_err());
        match result.unwrap_err() {
            SubSwapError::CodexRunning(pids) => assert_eq!(pids, vec![99999]),
            other => panic!("Expected CodexRunning, got: {:?}", other),
        }
    }
}
```

**Section 3: How to Add a New Test**

`## How to Add a New Test` (per D-11) as a numbered recipe:

1. **Choose test location:**
   - Testing a single function → unit test in the source file's `#[cfg(test)] mod tests` block
   - Testing CLI behavior end-to-end → integration test in `tests/integration.rs`
   - Enforcing a structural rule → structural test in `tests/arch.rs`

2. **Set up temp paths** (if test does file I/O):
   ```rust
   let tmp = TempDir::new().unwrap();
   let paths = Paths::from_temp(tmp.path());
   std::fs::create_dir_all(&paths.sub_swap_dir).unwrap();
   std::fs::create_dir_all(&paths.codex_dir).unwrap();
   std::fs::create_dir_all(paths.profiles_dir()).unwrap();
   ```

3. **Mock external dependencies:**
   - Keychain access → `MockKeyStore::new()` (implements `KeyStore` trait)
   - Process detection → `MockGuard::new(vec![])` (implements `CodexGuard` trait)

4. **Write assertions** — use `assert_eq!`, `assert!(result.is_ok())`, or pattern match on error variants

5. **Run:** `cargo test --lib module::tests::test_name` for a single unit test, `cargo test --test integration` for integration tests, `cargo test --test arch` for structural tests

**Section 4: Adding Architectural Rules**

`## Adding Architectural Rules` (per D-12) as a recipe:

To add a new layer boundary rule (e.g., "`profile/store.rs` must not import `tui`"):

1. Open `tests/arch.rs`
2. Add a new `#[test]` function:
   ```rust
   #[test]
   fn arch_01_profile_store_does_not_import_tui() {
       let source = read_source("src/profile/store.rs");
       assert_no_crate_import("src/profile/store.rs", &source, &["tui"]);
   }
   ```
3. Run `cargo test --test arch` to verify it passes on the current codebase
4. The test uses prefix matching: checks for `use crate::{module}::` and `use crate::{module};`
5. Failure messages follow the 3-part format: VIOLATION / FOUND / HOW TO FIX

To add a new purity check (e.g., forbid `std::net` in a module):
1. Add a `#[test]` function that reads the source file and scans for the forbidden string pattern
2. Follow the arch_02 pattern in `tests/arch.rs`

To add a new network crate to the deny-list:
1. Add the crate name to the `NETWORK_CRATES` constant at the top of `tests/arch.rs`

**Section 5: Test Location Map**

`## Test Location Map` with a table:

| Test Type | Location | Run Command |
|-----------|----------|-------------|
| Unit tests (all) | `src/**/*.rs` inside `#[cfg(test)] mod tests` | `cargo test --lib` |
| Unit tests (module) | e.g., `src/crypto/mod.rs` | `cargo test --lib crypto::tests` |
| Unit tests (single) | e.g., `src/crypto/mod.rs::test_encrypt_then_decrypt_roundtrip` | `cargo test --lib crypto::tests::test_encrypt_then_decrypt_roundtrip` |
| Integration tests | `tests/integration.rs` | `cargo test --test integration` |
| Structural tests | `tests/arch.rs` | `cargo test --test arch` |
| All tests | All of the above | `cargo test` |
| Validate (fmt + lint + test) | justfile recipe | `just validate` |
  </action>
  <verify>
    <automated>test -f docs/TESTING.md && grep -c "## Constraints" docs/TESTING.md && grep -c "from_temp" docs/TESTING.md && grep -c "MockKeyStore" docs/TESTING.md && grep -c "MockGuard" docs/TESTING.md && grep -c "assert_no_crate_import" docs/TESTING.md && grep -c "How to Add" docs/TESTING.md && wc -l < docs/TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/TESTING.md exists
    - Contains "## Constraints" section header within the first 50 lines
    - Contains "from_temp" (Paths injection template)
    - Contains "MockKeyStore" with copyable code template inside a rust code block
    - Contains "MockGuard" with copyable code template inside a rust code block
    - Contains "#[cfg(test)]" warning about test-only availability
    - Contains "## How to Add a New Test" recipe section
    - Contains "## Adding Architectural Rules" recipe section
    - Contains "assert_no_crate_import" (arch.rs extension pattern)
    - Contains "## Test Location Map" with run commands
    - Contains "cargo test --lib" and "cargo test --test arch" and "cargo test --test integration"
    - File is at least 120 lines
  </acceptance_criteria>
  <done>docs/TESTING.md exists with constraints-first structure, three copyable test templates (Paths::from_temp, MockKeyStore, MockGuard), step-by-step "How to Add a New Test" recipe, arch rule extension recipe, and test location map</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 ADRs in docs/decisions/</name>
  <files>docs/decisions/001-aes-256-gcm.md, docs/decisions/002-os-keychain.md, docs/decisions/003-path-injection.md, docs/decisions/004-offline-only.md</files>
  <read_first>
    - src/crypto/mod.rs (AES-256-GCM implementation details for ADR 001)
    - src/crypto/keychain.rs (KeyStore trait, keyring crate usage for ADR 002)
    - src/paths.rs (Paths struct, from_temp for ADR 003)
    - Cargo.toml (dependency list, absence of network crates for ADR 004)
    - tests/arch.rs (arch_03 test enforcing offline-only for ADR 004)
    - .planning/phases/03-documentation-knowledge-base/03-RESEARCH.md (§ADR Content Summary for verified content per ADR)
  </read_first>
  <action>
Create the `docs/decisions/` directory and write exactly 4 ADR files (per D-03, D-04 format, D-05 filenames):

**ADR format (per D-04):** Each ADR uses: Title, Status, Date, Context, Decision, Consequences, Implementation. Each ADR is under 100 lines.

**File 1: `docs/decisions/001-aes-256-gcm.md`**

```markdown
# ADR-001: AES-256-GCM for Profile Encryption

**Status:** Accepted
**Date:** 2026-04-02

## Context

sub-swap stores inactive Codex profile credentials (auth.json, config.toml) on disk. These files contain API tokens and configuration that must be protected at rest. The encryption scheme must provide both confidentiality and tamper detection — if an encrypted file is modified, decryption must fail rather than silently produce corrupted output.

## Decision

Use AES-256-GCM (Galois/Counter Mode) via the `aes-gcm` crate with:
- 256-bit key (32 bytes) generated from OS CSPRNG
- 96-bit nonce (12 bytes) randomly generated per encryption operation
- Output format: `[12-byte nonce][ciphertext][16-byte GCM authentication tag]`
- Each file encrypted independently (not bundled)
- Pure functions: `encrypt(plaintext, key) -> Result<Vec<u8>>` and `decrypt(data, key) -> Result<Vec<u8>>`

## Consequences

**Easier:**
- Tamper detection is automatic (GCM authentication tag)
- Per-operation random nonce eliminates nonce-reuse risk for reasonable usage patterns
- Pure function design makes encrypt/decrypt testable without mocking

**Harder:**
- Key loss means encrypted profiles are permanently unrecoverable
- No key rotation mechanism (would require decrypting and re-encrypting all profiles)

**Constrained:**
- Minimum ciphertext size is 28 bytes (12 nonce + 16 tag) — decrypt rejects anything smaller
- The `aes-gcm` crate is a required dependency

## Implementation

- `src/crypto/mod.rs` — `encrypt()`, `decrypt()`, `generate_key()` functions
- `src/crypto/keychain.rs` — Key storage via `KeyStore` trait
```

**File 2: `docs/decisions/002-os-keychain.md`**

```markdown
# ADR-002: OS Keychain for Encryption Key Storage

**Status:** Accepted
**Date:** 2026-04-02

## Context

The 256-bit encryption key must be stored somewhere the application can retrieve it, but NOT on the filesystem alongside the encrypted files. Storing the key in a dotfile would defeat the purpose of encryption — anyone with filesystem access could read both the key and the encrypted data.

## Decision

Store the encryption key in the operating system's native keychain via the `keyring` crate:
- Service name: `"sub-swap"`
- Account name: `"encryption-key"`
- Key format: hex-encoded (64 character string)
- Platform backends: macOS Keychain Services, Linux secret-service (GNOME Keyring / KWallet), Windows Credential Manager
- Abstracted behind `KeyStore` trait with `get_key()` and `set_key()` methods

## Consequences

**Easier:**
- Key is protected by OS-level access controls (user login required)
- Cross-platform via single crate abstraction
- No filesystem exposure of key material
- `KeyStore` trait enables `MockKeyStore` for testing without real keychain access

**Harder:**
- Key is machine-bound — migrating profiles between machines requires manual key transfer
- Linux requires a running secret-service daemon (GNOME Keyring or KWallet)

**Constrained:**
- Key loss = unrecoverable encrypted profiles (no escrow or backup mechanism)
- `keyring` crate is a required dependency

## Implementation

- `src/crypto/keychain.rs` — `KeyStore` trait, `OsKeyStore` (production), `MockKeyStore` (tests)
- `OsKeyStore` wraps `keyring::Entry::new("sub-swap", "encryption-key")`
```

**File 3: `docs/decisions/003-path-injection.md`**

```markdown
# ADR-003: Path Injection for Testability

**Status:** Accepted
**Date:** 2026-04-02

## Context

sub-swap reads and writes files in two fixed locations: `~/.codex/` (active profile) and `~/.sub-swap/` (stored profiles, config, index). Functions hardcoding these paths would be untestable — tests would modify the user's real home directory.

## Decision

Inject a `Paths` struct into every function that performs filesystem I/O:
- `Paths` holds `codex_dir` and `sub_swap_dir` fields plus derived methods (`profiles_dir()`, etc.)
- Production: `Paths::new()` resolves to real home directory paths
- Tests: `Paths::from_temp(tmp.path())` maps to a temp directory (`#[cfg(test)]` gated)
- All filesystem functions take `&Paths` as a parameter instead of computing paths internally

## Consequences

**Easier:**
- 100% of filesystem operations are testable without touching the real home directory
- Production and test code share identical function signatures — no conditional compilation in business logic
- Adding new file paths is straightforward (add a method to `Paths`)

**Harder:**
- Every function that does file I/O must accept a `&Paths` parameter (threading it through call chains)
- Tests must manually create subdirectories after calling `from_temp` (it creates the mapping, not the directories)

**Constrained:**
- `Paths::from_temp` is `#[cfg(test)]` gated — cannot be used outside test builds
- `tempfile` crate is a dev-dependency for `TempDir`

## Implementation

- `src/paths.rs` — `Paths` struct with `new()` and `from_temp()` constructors
- Used in: `src/config.rs`, `src/profile/store.rs`, `src/profile/switch.rs`
- Test pattern: `let tmp = TempDir::new().unwrap(); let paths = Paths::from_temp(tmp.path());`
```

**File 4: `docs/decisions/004-offline-only.md`**

```markdown
# ADR-004: Offline-Only Constraint

**Status:** Accepted
**Date:** 2026-04-02

## Context

sub-swap is a credential manager — it handles API tokens and authentication files. Network-capable code in a credential manager dramatically increases the attack surface: data exfiltration, man-in-the-middle attacks, and unintended telemetry all become possible. The design principle is that a tool managing secrets should have the smallest possible attack surface.

## Decision

Zero network crates in the entire dependency tree:
- No HTTP clients (`reqwest`, `hyper`, `surf`, `ureq`, `attohttpc`, `isahc`, `curl`)
- No WebSocket libraries (`tungstenite`, `websocket`)
- No async runtimes (`tokio`, `async-std`, `smol`) — these imply network capability
- All operations are synchronous
- Enforced by `tests/arch.rs` arch_03 test which deny-lists 13 network/async crates in `[dependencies]` and `[dev-dependencies]`

## Consequences

**Easier:**
- Attack surface is limited to local filesystem and OS keychain
- No network-related CVEs can affect the application
- Simpler mental model — all operations are local and synchronous
- `arch_03_no_network_crates_in_dependencies` prevents accidental re-introduction

**Harder:**
- Cross-machine profile sharing is impossible without a future export/import feature
- No auto-update mechanism
- No remote backup capability

**Constrained:**
- Adding any network crate will cause `cargo test` to fail (arch_03 enforced)
- Overriding requires modifying the `NETWORK_CRATES` constant in `tests/arch.rs` and justifying the addition

## Implementation

- `Cargo.toml` — absence of network crates (enforced by test)
- `tests/arch.rs` — `arch_03_no_network_crates_in_dependencies` test with `NETWORK_CRATES` deny-list
```

Create the `docs/decisions/` directory first, then write all four files.
  </action>
  <verify>
    <automated>test -d docs/decisions && ls docs/decisions/ | wc -l | tr -d ' ' && test -f docs/decisions/001-aes-256-gcm.md && test -f docs/decisions/002-os-keychain.md && test -f docs/decisions/003-path-injection.md && test -f docs/decisions/004-offline-only.md && grep -c "## Decision" docs/decisions/001-aes-256-gcm.md && grep -c "## Decision" docs/decisions/002-os-keychain.md && grep -c "## Decision" docs/decisions/003-path-injection.md && grep -c "## Decision" docs/decisions/004-offline-only.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/decisions/ directory exists
    - docs/decisions/ contains exactly 4 files
    - docs/decisions/001-aes-256-gcm.md exists and contains "## Decision" and "AES-256-GCM" and "src/crypto/mod.rs"
    - docs/decisions/002-os-keychain.md exists and contains "## Decision" and "keyring" and "src/crypto/keychain.rs"
    - docs/decisions/003-path-injection.md exists and contains "## Decision" and "Paths" and "from_temp" and "src/paths.rs"
    - docs/decisions/004-offline-only.md exists and contains "## Decision" and "NETWORK_CRATES" and "tests/arch.rs"
    - All 4 ADRs contain: "Status:", "Date:", "## Context", "## Decision", "## Consequences", "## Implementation"
    - All 4 ADRs are under 100 lines each
    - No ADR references CLAUDE.md (self-contained per D-13)
  </acceptance_criteria>
  <done>docs/decisions/ contains exactly 4 ADR files (001-aes-256-gcm.md, 002-os-keychain.md, 003-path-injection.md, 004-offline-only.md), each with standard ADR format (Status, Date, Context, Decision, Consequences, Implementation), each under 100 lines, each referencing specific implementation source files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan creates documentation files only — no executable code. No trust boundaries are crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | ADR files | accept | ADRs describe design rationale — they contain no secrets. Algorithm parameters and crate names are public knowledge. |
| T-03-02 | Tampering | docs/TESTING.md | accept | Test templates are copyable patterns, not executable infrastructure. Inaccuracy is a quality risk, not a security risk. Verified against actual source. |
</threat_model>

<verification>
After both tasks complete:
1. `test -f docs/TESTING.md` — file exists
2. `ls docs/decisions/ | wc -l` — outputs 4
3. `head -50 docs/TESTING.md | grep -c "Constraints"` — constraints section in first 50 lines
4. `cargo test` — existing test suite still passes (safety net)
</verification>

<success_criteria>
- docs/TESTING.md exists with constraints-first structure, three copyable templates, two recipes, and a test location map
- docs/decisions/ contains exactly 4 ADR files with correct filenames (001-004)
- Each ADR follows the standard format (Status, Date, Context, Decision, Consequences, Implementation)
- Each ADR is under 100 lines and references specific source files
- TESTING.md clearly states #[cfg(test)] requirement for all three test abstractions
- All files are self-contained (per D-13)
- `cargo test` still passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-knowledge-base/03-02-SUMMARY.md`
</output>
