---
phase: 03-documentation-knowledge-base
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ARCHITECTURE.md
  - docs/SECURITY.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - "An agent reading docs/ARCHITECTURE.md can identify which layer a module belongs to and what it may import without reading any source code"
    - "An agent reading docs/SECURITY.md can understand the encryption model, key lifecycle, threat boundaries, and file permission rules without reading crypto/ source"
    - "Both documents lead with constraints and invariants within the first 50 lines"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "Module layout, dependency graph, layer boundaries, arch.rs enforcement documentation"
      min_lines: 120
      contains: "## Constraints"
    - path: "docs/SECURITY.md"
      provides: "Encryption model, key management, threat model, file permissions, switch lifecycle"
      min_lines: 150
      contains: "## Constraints"
  key_links:
    - from: "docs/ARCHITECTURE.md"
      to: "tests/arch.rs"
      via: "documents what arch.rs enforces and the single-line import limitation"
      pattern: "single-line"
    - from: "docs/SECURITY.md"
      to: "src/crypto/mod.rs"
      via: "documents the nonce|ciphertext|tag format and AES-256-GCM parameters"
      pattern: "AES-256-GCM"
    - from: "docs/SECURITY.md"
      to: "src/profile/switch.rs"
      via: "documents the 14-step switch lifecycle including backup/restore"
      pattern: "backup"
---

<objective>
Create docs/ARCHITECTURE.md and docs/SECURITY.md as agent-legible, self-contained reference documents.

Purpose: These two documents give AI agents the architectural and security context needed to modify code safely — layer boundaries, dependency rules, encryption model, key management, threat scope, and file permission constraints.

Output: Two Markdown files in the docs/ directory, each leading with constraints in the first 50 lines (per D-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documentation-knowledge-base/03-CONTEXT.md
@.planning/phases/03-documentation-knowledge-base/03-RESEARCH.md
@.planning/phases/02-architectural-enforcement/02-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/ARCHITECTURE.md</name>
  <files>docs/ARCHITECTURE.md</files>
  <read_first>
    - .planning/phases/02-architectural-enforcement/02-CONTEXT.md (verified dependency map in §code_context — this is the SOURCE OF TRUTH per D-07)
    - tests/arch.rs (structural test names, the single-line import assumption comment at line ~8, and the helper function signatures)
    - src/lib.rs (module re-export list defining public API surface)
    - CLAUDE.md (existing architecture description — must not contradict)
    - .planning/phases/03-documentation-knowledge-base/03-RESEARCH.md (§Architecture Patterns for document structure, §Content Inventory for verified content)
  </read_first>
  <action>
Create `docs/ARCHITECTURE.md` with the following exact structure and content (per D-01 agent-first, D-02 tables over prose, D-06 required content, D-07 verified dependency map):

**Section 1: Constraints and Invariants (lines 1-50)**

Open with `# Architecture` title, then a `## Constraints and Invariants` section containing a table:

| Rule | Enforced By | Consequence of Violation |
|------|-------------|--------------------------|
| Foundation modules import only `std`/external crates (exception: `paths` may import `error`) | `tests/arch.rs` arch_01 tests (9 tests) | `cargo test` fails with VIOLATION/FOUND/HOW TO FIX message |
| Core modules import only Foundation | `tests/arch.rs` arch_01 tests | `cargo test` fails |
| Business modules import Core + Foundation; never Orchestration or `guard` | `tests/arch.rs` arch_01 tests | `cargo test` fails |
| Orchestration modules may import anything | No restriction | N/A |
| `crypto/mod.rs` has no filesystem I/O (`std::fs`, `std::io::Write`, `std::net`, `std::process`) | `tests/arch.rs` arch_02 test | `cargo test` fails |
| `crypto/keychain.rs` is exempt from purity — side effects are its purpose | Documented exemption (D-04 from Phase 2) | N/A |
| Zero network crates in `[dependencies]` or `[dev-dependencies]` | `tests/arch.rs` arch_03 test | `cargo test` fails |
| All `use crate::` imports must be single-line (not multi-line grouped) | Structural test assumption | Multi-line grouped imports bypass boundary detection |

Follow with a boxed warning about the single-line import limitation:
```
> **WARNING:** `tests/arch.rs` parses `use crate::` statements assuming single-line imports.
> If multi-line grouped imports (e.g., `use crate::{foo, bar}`) are introduced, boundary
> tests may miss violations. See the comment at the top of `tests/arch.rs`.
```

**Section 2: Module Layout**

`## Module Layout` with an ASCII diagram showing the 4-layer structure:

```
Orchestration    cli    tui/mod    tui/wizard    tui/widgets
                  |        |           |              |
Business        profile/mod   profile/store   profile/switch
                  |               |                |
Core           crypto/mod  crypto/keychain  config  guard
                  |               |            |      |
Foundation     error                        paths
```

**Section 3: Dependency Map**

`## Dependency Map` with the EXACT table from 02-CONTEXT.md §code_context (the verified source of truth per D-07):

| Module | Imports From | Layer |
|--------|-------------|-------|
| `error` | (nothing internal) | Foundation |
| `paths` | `error` | Foundation |
| `crypto/mod.rs` | `error` | Core |
| `crypto/keychain.rs` | `error` | Core |
| `config` | `error`, `paths` | Core |
| `guard` | `error` | Core |
| `profile/mod.rs` | `error` | Business |
| `profile/store.rs` | `error`, `paths`, `profile` | Business |
| `profile/switch.rs` | `crypto`, `error`, `paths`, `profile` | Business |
| `cli` | `config`, `crypto`, `error`, `guard`, `paths`, `profile` | Orchestration |
| `tui/mod.rs` | `config`, `crypto`, `error`, `guard`, `paths`, `profile` | Orchestration |
| `tui/wizard.rs` | `config`, `crypto`, `error`, `paths`, `profile` | Orchestration |
| `tui/widgets.rs` | `profile` | Orchestration |

**Section 4: Layer Definitions**

`## Layer Definitions` with a subsection for each layer (Foundation, Core, Business, Orchestration). Each subsection lists the modules, their responsibility, and the specific import rules. Use brief prose for rationale — e.g., Foundation exists to provide error types and path abstractions with zero coupling to business logic.

**Section 5: Boundary Enforcement**

`## Boundary Enforcement` explaining how `tests/arch.rs` works:
- `read_source(path)` reads the source file at test runtime
- `assert_no_crate_import(file, source, forbidden)` scans for `use crate::{module}::` and `use crate::{module};` patterns
- `dep_names(table)` parses `Cargo.toml` via `toml::Table` to extract dependency crate names
- List all 11 test function names with one-line descriptions (copy from RESEARCH.md §ARCHITECTURE.md Content)
- Note: `crypto/keychain.rs` is exempt from purity checks (D-04)

**Section 6: Entry Points**

`## Entry Points` briefly describing the two entry modes (CLI via `cli.rs` with clap derive, TUI via `tui/mod.rs` with ratatui state machine, first-launch wizard via `tui/wizard.rs`).

**Section 7: External Dependencies**

`## External Dependencies` with a table of key crates from Cargo.toml and WHY they exist:

| Crate | Purpose | Why Not Hand-Rolled |
|-------|---------|---------------------|
| `aes-gcm` | AES-256-GCM encryption | Timing-safe, audited |
| `keyring` | OS keychain abstraction | macOS/Linux/Windows portability |
| `sysinfo` | Process detection | Cross-platform, no shell-out |
| `clap` | CLI argument parsing | Derive macro for type-safe subcommands |
| `ratatui` | Terminal UI framework | State machine rendering |
| `toml` | TOML parsing | Config file + Cargo.toml parsing in tests |
| `serde`/`serde_json` | Serialization | profiles.json, auth.json |
| `rand` | CSPRNG | Nonce generation, key generation |

Do NOT include a "Related Documents" section pointing to CLAUDE.md (per D-13, self-contained).
  </action>
  <verify>
    <automated>test -f docs/ARCHITECTURE.md && grep -c "## Constraints" docs/ARCHITECTURE.md && grep -c "single-line" docs/ARCHITECTURE.md && grep -c "Foundation" docs/ARCHITECTURE.md && grep -c "Orchestration" docs/ARCHITECTURE.md && grep -c "arch_01" docs/ARCHITECTURE.md && grep -c "arch_02" docs/ARCHITECTURE.md && grep -c "arch_03" docs/ARCHITECTURE.md && wc -l < docs/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/ARCHITECTURE.md exists
    - Contains "## Constraints" section header
    - Contains "single-line" warning about arch.rs limitation
    - Contains all 4 layer names: "Foundation", "Core", "Business", "Orchestration"
    - Contains all 13 module entries from the dependency map table
    - Contains all 11 arch test function names (arch_01_*, arch_02_*, arch_03_*)
    - Contains the external dependencies table with at least aes-gcm, keyring, sysinfo
    - File is at least 120 lines
    - "## Constraints" section appears within the first 50 lines
  </acceptance_criteria>
  <done>docs/ARCHITECTURE.md exists with constraints-first structure, complete dependency map matching 02-CONTEXT.md, all 11 arch.rs test names documented, single-line import limitation called out, and external dependency rationale table</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/SECURITY.md</name>
  <files>docs/SECURITY.md</files>
  <read_first>
    - src/crypto/mod.rs (encrypt/decrypt function signatures, nonce size, output format, minimum length check)
    - src/crypto/keychain.rs (KeyStore trait, OsKeyStore, service name "sub-swap", account "encryption-key", key size, hex encoding)
    - src/profile/switch.rs (the full 14-step switch lifecycle including backup/restore mechanism)
    - src/profile/store.rs (0600 permissions, file I/O patterns)
    - src/config.rs (0600 permissions on config.json)
    - src/error.rs (validate_profile_name rules)
    - .planning/phases/03-documentation-knowledge-base/03-RESEARCH.md (§SECURITY.md Content for verified content inventory, §Common Pitfalls for accuracy traps)
  </read_first>
  <action>
Create `docs/SECURITY.md` with the following exact structure and content (per D-01 agent-first, D-02 tables, D-08 required content, D-09 switch lifecycle):

**Section 1: Constraints and Invariants (lines 1-50)**

Open with `# Security` title, then `## Constraints and Invariants` containing a table:

| Constraint | Implementation | Verified By |
|------------|----------------|-------------|
| Inactive profiles encrypted with AES-256-GCM at rest | `src/crypto/mod.rs` encrypt/decrypt | Unit tests in `crypto::tests` |
| Encryption key never written to filesystem | `src/crypto/keychain.rs` stores in OS keychain | `KeyStore` trait + `MockKeyStore` in tests |
| All files under `~/.sub-swap/` are mode 0600 (Unix) | `#[cfg(unix)]` blocks in `config.rs`, `profile/store.rs`, `profile/switch.rs` | Manual — no automated permission tests |
| Zero network crates in dependency tree | `Cargo.toml` has no network crates | `tests/arch.rs` arch_03 test |
| Active profile stays plaintext in `~/.codex/` | Required by Codex to read credentials | N/A — design constraint |
| Profile names validated against path traversal | `validate_profile_name()` in `error.rs` | Unit tests in `error::tests` |

Follow with a boxed note:
```
> **IMPORTANT:** The active profile's credentials (`~/.codex/auth.json`) are always plaintext.
> sub-swap protects *inactive* profiles only. This is a design constraint — Codex must be
> able to read its own config files without decryption.
```

**Section 2: Encryption Model**

`## Encryption Model` with:
- Algorithm: AES-256-GCM (authenticated encryption with associated data)
- Nonce: 96-bit (12 bytes), randomly generated per encryption call via OS CSPRNG (`rand::rng().fill_bytes`)
- Output format: `[12-byte nonce][ciphertext][16-byte GCM authentication tag]` — concatenated into a single `Vec<u8>`
- Each file (`auth.json`, `config.toml`) encrypted independently
- Function signatures (copy exactly from source):
  - `pub fn encrypt(plaintext: &[u8], key: &[u8; 32]) -> Result<Vec<u8>>`
  - `pub fn decrypt(data: &[u8], key: &[u8; 32]) -> Result<Vec<u8>>`
- Decryption validates minimum length: 28 bytes (12 nonce + 16 tag)
- Source file: `src/crypto/mod.rs`

**Section 3: Key Management**

`## Key Management` with:
- Key size: 256-bit (32 bytes)
- Generation: `generate_key()` using `rand::rng().fill_bytes` (OS CSPRNG)
- Storage: hex-encoded (64 character string) in OS keychain via `keyring` crate
- Keychain identifiers: service = `"sub-swap"`, account = `"encryption-key"`
- Platform backends table:

| Platform | Backend |
|----------|---------|
| macOS | Keychain Services |
| Linux | secret-service (GNOME Keyring / KWallet) |
| Windows | Credential Manager |

- Key lifecycle: generated once on first profile add with encryption enabled, retrieved on every encrypt/decrypt operation
- `get_or_default_key()`: returns zeroed 32-byte array when encryption is disabled (bypasses keychain call entirely)
- Key loss: encrypted profiles become unrecoverable (no backup/escrow mechanism)
- Source file: `src/crypto/keychain.rs`

**Section 4: Threat Model**

`## Threat Model` with two subsections:

`### What Is Protected`
- Inactive credentials encrypted at rest with AES-256-GCM (confidentiality + integrity)
- Encryption key stored in OS keychain, never on filesystem (key separation)
- All files 0600 on Unix — owner-only access (`#[cfg(unix)]` gated; no-op on Windows)
- Zero network attack surface (no network crates; enforced by `tests/arch.rs` arch_03)
- Path traversal blocked by `validate_profile_name()` (rejects `/`, `\`, `..`, leading `.`; allows only alphanumeric + `-` + `_`)

`### What Is NOT Protected`
- Active profile credentials in `~/.codex/auth.json` are plaintext (Codex requires this)
- No memory zeroization — keys and plaintext exist in heap memory until dropped
- No protection against root-level or same-user access (OS keychain provides user-level isolation only)
- Key loss = unrecoverable encrypted profiles (no key backup mechanism)
- No protection against malicious code running as the same user
- `profiles.json` is not encrypted (contains metadata only — profile names, active flag) but IS 0600

**Section 5: Profile Switch Lifecycle**

`## Profile Switch Lifecycle` (per D-09) documenting the actual 14-step implementation from `src/profile/switch.rs`:

1. Load `ProfileStore` from `profiles.json`
2. If target is already active -> return Ok (no-op)
3. If target doesn't exist in index -> return `ProfileNotFound` error
4. Load target profile files from `~/.sub-swap/profiles/<target>/`
5. Detect if encrypted (`.enc` suffix present on files)
6. Decrypt target files in memory if encrypted
7. Read current active profile from `~/.codex/`
8. Encrypt old active profile files (if `encrypt=true`), write to `~/.sub-swap/profiles/<old>/`
9. Create backups: `~/.codex/auth.json.bak` and `~/.codex/config.toml.bak`
10. Write target plaintext to `~/.codex/auth.json` and `~/.codex/config.toml`
11. Set 0600 permissions on codex files (Unix)
12. Update `profiles.json` index: set_active(target), save
13. On success: remove backup files
14. On any failure in steps 10-12: restore from backups, remove backups, return error

Follow the list with a `### Atomicity Note` explaining: The backup-and-restore mechanism (steps 9, 13, 14) provides the atomic-swap property. There is a brief window between steps 9 and 10 where both backup and active codex files exist simultaneously. If the process is killed in this window, backup files will remain and must be cleaned up manually.

**Section 6: File Permissions**

`## File Permissions` with a table:

| File | Location | Encrypted | Mode (Unix) | Notes |
|------|----------|-----------|-------------|-------|
| `profiles.json` | `~/.sub-swap/` | No | 0600 | Metadata only (profile names, active flag) |
| `config.json` | `~/.sub-swap/` | No | 0600 | App configuration |
| `auth.json(.enc)` | `~/.sub-swap/profiles/<name>/` | Yes (inactive) | 0600 | Codex API credentials |
| `config.toml(.enc)` | `~/.sub-swap/profiles/<name>/` | Yes (inactive) | 0600 | Codex configuration |
| `auth.json` | `~/.codex/` | No (active) | 0600 | Active profile — plaintext for Codex |
| `config.toml` | `~/.codex/` | No (active) | 0600 | Active profile — plaintext for Codex |

Note: 0600 is applied via `fs::Permissions::from_mode(0o600)` inside `#[cfg(unix)]` blocks. On Windows, this code is a no-op.

**Section 7: Input Validation**

`## Input Validation` documenting `validate_profile_name()` from `src/error.rs`:
- Rules: non-empty, no `/`, no `\`, no `..`, no leading `.`, characters restricted to alphanumeric + `-` + `_`
- Called at: CLI dispatch (before any operation) and in `add_profile_from_*` functions
- Prevents: directory traversal attacks (e.g., `../../etc/passwd` as a profile name)
  </action>
  <verify>
    <automated>test -f docs/SECURITY.md && grep -c "## Constraints" docs/SECURITY.md && grep -c "AES-256-GCM" docs/SECURITY.md && grep -c "0600" docs/SECURITY.md && grep -c "backup" docs/SECURITY.md && grep -c "validate_profile_name" docs/SECURITY.md && grep -c "Threat Model" docs/SECURITY.md && wc -l < docs/SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/SECURITY.md exists
    - Contains "## Constraints" section header within the first 50 lines
    - Contains "AES-256-GCM" (encryption algorithm)
    - Contains "0600" (file permissions)
    - Contains "backup" (switch lifecycle atomicity)
    - Contains "validate_profile_name" (input validation)
    - Contains "## Threat Model" section header
    - Contains "## Profile Switch Lifecycle" section header
    - Contains "## Key Management" section header
    - Contains both function signatures: "pub fn encrypt" and "pub fn decrypt"
    - Contains "sub-swap" and "encryption-key" (keychain identifiers)
    - Contains "#[cfg(unix)]" qualifier for the 0600 constraint
    - File is at least 150 lines
  </acceptance_criteria>
  <done>docs/SECURITY.md exists with constraints-first structure, complete encryption model with function signatures, key management with keychain identifiers, pragmatic threat model (protected vs not-protected), 14-step switch lifecycle with atomicity note, file permissions table with #[cfg(unix)] qualifier, and input validation rules</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan creates documentation files only — no executable code. No trust boundaries are crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | docs/SECURITY.md | accept | Security docs describe the threat model but contain no secrets. The encryption algorithm, nonce size, and keychain identifiers are not sensitive — they are standard parameters. Security depends on key secrecy (in OS keychain), not algorithm secrecy. |
| T-03-02 | Tampering | docs/ARCHITECTURE.md | accept | Documentation accuracy is verified by cross-referencing source files. Inaccurate docs are a quality risk, not a security risk. Existing `tests/arch.rs` enforces the actual boundaries regardless of what docs say. |
</threat_model>

<verification>
After both tasks complete:
1. `test -f docs/ARCHITECTURE.md && test -f docs/SECURITY.md` — both files exist
2. `head -50 docs/ARCHITECTURE.md | grep -c "Constraints"` — constraints section in first 50 lines
3. `head -50 docs/SECURITY.md | grep -c "Constraints"` — constraints section in first 50 lines
4. `cargo test` — existing test suite still passes (safety net; docs shouldn't break tests)
</verification>

<success_criteria>
- docs/ARCHITECTURE.md exists with constraints-first structure, complete 13-row dependency map, all 11 arch.rs test names, single-line import warning, and external dependency rationale
- docs/SECURITY.md exists with constraints-first structure, encryption model, key management, threat model, 14-step switch lifecycle with atomicity note, file permissions table, and input validation rules
- Both files lead with constraints within the first 50 lines (per D-01)
- Both files use tables for quick-reference data (per D-02)
- Neither file depends on CLAUDE.md (per D-13)
- `cargo test` still passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-knowledge-base/03-01-SUMMARY.md`
</output>
